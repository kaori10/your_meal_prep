<div class="container">
	<div class="row">
		<div class="col-lg-12">
		    <ul class="nav nav-tabs nav-justified">
		        <li role="presentation" class="active">
		          　<%= link_to(recipes_genre_path(@genre), class: 'links') do %>
				        <%= @genre.name %>
		          　<% end %>
		        </li>
		        <% @genres.each do |genre| %>
		          　<li role="presentation" >
			            <%= link_to(recipes_genre_path(genre), class: 'links') do %>
				            <%= genre.name %>
			            <% end %>
		          　</li>
		        <% end %>
		    </ul>
		</div>
	</div>
	<div class="row">
		<div class="col-lg-4">
			<div class="heading06"><i class="fas fa-utensils"></i> Recipes</div>
		</div>
	</div>
	<%= render 'public/recipes/index', recipe: @recipe %>
</div>