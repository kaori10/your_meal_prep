<div class="container">
	<div class="row">
		<div class="col-lg-12">
			<h1>お気に入り</h1>
			<% if @favorite_list.present? %>
				<% @favorite_list.each do |recipe| %>
				<%= recipe.title %>
				<% end %>
			<% end %>
		</div>
	</div>
</div>
