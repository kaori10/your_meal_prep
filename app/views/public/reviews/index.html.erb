<div class="container">
	<div class="row">
		<div class="col-lg-12">
			<% @reviews.each do |review| %>
			<%= review.user.nickname %>
			<%= review.score %>
			<%= review.content %>
			<% end%>
		</div>
	</div>
</div>